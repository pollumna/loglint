version: "2"

linters:
  default: none
  enable:
    - loglint
  settings:
    custom:
      loglint:
        type: "module"
        description: Check log messages according to style rules

issues:
  max-same-issues: 0